version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.11"
  jobs:
    post_checkout:
      - git submodule sync --recursive
      - git submodule update --init --recursive
    pre_build:
      - python readthedocs/scripts/build_docs.py

submodules:
  include: all
  recursive: true

python:
  install:
    # 使用锁定版本，避免依赖小版本升级导致构建产物变化。
    - requirements: readthedocs/requirements.lock.txt

mkdocs:
  configuration: readthedocs/mkdocs.yml

formats: []
